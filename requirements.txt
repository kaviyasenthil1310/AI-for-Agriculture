# Web Framework
fastapi==0.141.1
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# Machine Learning & Image Processing
# The pinned versions above were invalid for the current Python 3.13 environment.
# Keep the Torch/TorchVision pair aligned and use a NumPy version compatible with this runtime.
torch==2.13.0
torchvision==0.28.0
pillow==10.4.0

# Data Processing
numpy>=2.1.0

# HTTP Clients
requests==2.32.3
httpx==0.27.2

# System Monitoring (optional but recommended)
psutil>=5.9.0
